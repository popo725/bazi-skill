"""Legacy deterministic fortune-calculation scripts.

Commercial-site code should access these modules through adapters/services rather
than importing implementation details directly from HTTP routes.
"""
